scripts:
  start: deno run --allow-net --allow-read=./ --allow-env=DENO_REGION serve.tsx
  dev: deno run --allow-env=DENO_REGION --allow-net --allow-read=./ --watch serve.tsx
  bundle-tixy: deno bundle --watch tixy/index.js tixy/bundle.js
  tixy:
    pll:
      - vr dev
      - vr bundle-tixy